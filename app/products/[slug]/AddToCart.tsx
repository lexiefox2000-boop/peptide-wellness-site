"use client";
import {useState} from "react";
import Link from "next/link";
import {Check,Minus,Plus} from "lucide-react";
import {useCart} from "@/app/CartProvider";
export default function AddToCart({slug}:{slug:string}){const{addItem}=useCart();const[q,setQ]=useState(1);const[added,setAdded]=useState(false);function add(){addItem(slug,q);setAdded(true);setTimeout(()=>setAdded(false),1800)}return <div className="mt-8"><div className="text-sm font-medium">Quantity</div><div className="mt-3 inline-flex items-center rounded-full border border-black/10 bg-white p-1"><button type="button" aria-label="Decrease quantity" onClick={()=>setQ(v=>Math.max(1,v-1))} className="flex h-10 w-10 items-center justify-center rounded-full"><Minus size={16}/></button><span className="w-12 text-center font-semibold">{q}</span><button type="button" aria-label="Increase quantity" onClick={()=>setQ(v=>Math.min(20,v+1))} className="flex h-10 w-10 items-center justify-center rounded-full"><Plus size={16}/></button></div><button type="button" onClick={add} className="mt-4 inline-flex w-full items-center justify-center gap-2 rounded-full bg-[var(--primary)] px-7 py-4 text-sm font-semibold text-white sm:w-auto sm:min-w-64">{added&&<Check size={17}/>} {added?"Added to cart":"Add to cart"}</button><div className="mt-3 flex gap-5 text-sm"><Link href="/cart" className="font-medium text-[var(--primary)]">View cart →</Link><Link href="/#products" className="text-[var(--muted)]">Keep shopping</Link></div></div>}
